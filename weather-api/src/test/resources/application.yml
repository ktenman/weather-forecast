spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

ilmateenistus:
  url: http://localhost:${wiremock.server.port}
